spring:
  datasource:
    name: chat
    url: jdbc:mysql://localhost:3306/chat
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
